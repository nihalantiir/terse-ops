---
name: solo
description: Disable delegation for the rest of this session — do everything directly instead of routing to scout/researcher/builder/checker/architect. Invoke directly as /terse-ops:solo.
disable-model-invocation: true
effort: low
---

Stop delegating to subagents for the remainder of this session, until told otherwise. Do every task directly yourself, including ones that `orchestrate`'s routing table would normally hand off.

Everything else stays in effect: harness, budget, economy, output, and the rest still apply — this only turns off the delegation mechanism, not the other rules.

Confirm the change in one line, then apply it starting with this reply.
